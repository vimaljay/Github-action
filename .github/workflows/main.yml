name: my-first23adsasd
on:
  push:
    branches:
       - main
workflow_dispatch:
inputs:
  name: "type your name"
jobs:
    greetings:
      runs-on: ubuntu-latest
      steps:
      - name: Print data
        run: echo "Input guy:"
     
